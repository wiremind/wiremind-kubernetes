apiVersion: "wiremind.fr/v1"
kind: ExpectedDeploymentScale
metadata:
  name: concerned-eds
  namespace: default
  labels:
    release: concerned
spec:
  deploymentName: concerned
  expectedScale: 3


---

apiVersion: "wiremind.fr/v1"
kind: ExpectedDeploymentScale
metadata:
  name: unconcerned-eds
  namespace: default
  labels:
    release: unconcerned
spec:
  deploymentName: unconcerned
  expectedScale: 3
